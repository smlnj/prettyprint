(* prettyprint/src/prettyprint-html.cm *)
(* CM Description File for HTML 3 rendering *)

Library

  structure HTMLRender

is

(* members *)

html-style.sml   (* -> HTMLStyle *)
render.sig       (* -> HTML_RENDER *)
render.sml       (* -> HTMLRender *)

(* imports *)

../base/base.cm              (* <- Token *)
../formatting/formatting.cm  (* <- Format, Measure, Formatting *)

$/basis.cm
$/html-lib.cm    (* <- HTML *)

